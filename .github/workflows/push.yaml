name: Test
on: [push]

jobs:
  test_job:
    runs-on: ubuntu-latest
    name: Run all tests
    steps:
      - run: npm run test
  lint_job:
    runs-on: ubuntu-latest
    name: Check linting
    steps:
      - run: npm lint
