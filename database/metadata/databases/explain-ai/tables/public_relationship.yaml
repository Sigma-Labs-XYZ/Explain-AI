table:
  name: relationship
  schema: public
object_relationships:
  - name: from
    using:
      foreign_key_constraint_on: from_id
  - name: to
    using:
      foreign_key_constraint_on: to_id
select_permissions:
  - role: user
    permission:
      columns:
        - priority
        - description
        - from_id
        - to_id
        - id
      filter: {}
